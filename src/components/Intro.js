// Dependencies
import React from 'react';
// Styles
import styles from 'styles/components/Intro.scss';

const Intro = () => (
  <p className={styles.intro}>
    To get started, edit <code className={styles.code}>src/App.js</code> or{' '}
    <code>src/Home.js</code> and save to reload.
  </p>
);

export default Intro;
